// config.js
const config = {
    backendUrl: process.env.REACT_APP_BACKEND_URL || 'http://localhost',
    backendPort: process.env.REACT_APP_BACKEND_PORT || '3001',
  };
  
  export default config;
  